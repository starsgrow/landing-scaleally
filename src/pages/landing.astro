---
// VSL Landing Page - Scaleally (Pure Video Sales Letter)
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Protocolo de Dominio de Mercado - Método Probado para Infoproductores 2025-2026</title>
    <meta name="description" content="Método probado para escalar marcas de infoproductores y desarrollo personal en 2025-2026. Domina tu mercado con estrategias comprobadas.">
    
    <!-- Calendly -->
    <script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    
    <!-- Import global styles -->
    <link rel="stylesheet" href="../styles/global.css">
    
    <!-- Import JavaScript -->
    <script src="../scripts/landing.js"></script>
    
    <style>
        :root {
            --primary: #0f172a;
            --secondary: #1e293b;
            --accent: #6366f1;
            --accent-hover: #4f46e5;
            --accent-light: #818cf8;
            --success: #10b981;
            --warning: #f59e0b;
            --error: #ef4444;
            --bg: #ffffff;
            --bg-secondary: #f8fafc;
            --bg-dark: #0f172a;
            --text: #1e293b;
            --text-light: #64748b;
            --text-white: #ffffff;
            --border: #e2e8f0;
            --border-light: #f1f5f9;
            --shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
            --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            --shadow-glow: 0 0 20px rgba(99, 102, 241, 0.3);
            --gradient-primary: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
            --gradient-secondary: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            --gradient-hero: linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #334155 100%);
            --gradient-accent: linear-gradient(135deg, #6366f1 0%, #818cf8 100%);
            --gradient-button: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: var(--bg);
            color: var(--text);
            line-height: 1.6;
            font-weight: 400;
            margin: 0;
            padding: 0;
            position: relative;
            min-height: 100vh;
        }

        /* Video en pantalla completa para móviles */
        .mobile-video-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background-color: #000;
            z-index: 9999;
            overflow: hidden;
        }

        .mobile-video-overlay.active {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .mobile-video-container {
            width: 100%;
            height: 100%;
            position: relative;
        }

        .mobile-video-player {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .close-video-button {
            position: absolute;
            top: 20px;
            right: 20px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            border: 2px solid white;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            font-size: 24px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 10000;
            backdrop-filter: blur(5px);
            transition: all 0.3s ease;
        }

        .close-video-button:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: scale(1.1);
        }

        .toggle-audio-button {
            position: absolute;
            bottom: 30px;
            right: 20px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            border: 2px solid white;
            border-radius: 50%;
            width: 60px;
            height: 60px;
            font-size: 28px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 10000;
            backdrop-filter: blur(5px);
            transition: all 0.3s ease;
        }

        .toggle-audio-button:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: scale(1.1);
        }


        /* Ocultar el contenido principal cuando el video está activo en móviles */
        body.video-playing {
            overflow: hidden;
        }

        /* Header */
        .header {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border-light);
            z-index: 1000;
            transition: all 0.3s ease;
        }

        .header.scrolled {
            background: rgba(255, 255, 255, 0.98);
            box-shadow: var(--shadow-lg);
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 80px;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            text-decoration: none;
            color: var(--text);
            font-weight: 800;
            font-size: 1.5rem;
            transition: transform 0.3s ease;
        }

        .logo:hover {
            transform: scale(1.05);
        }

        .logo img {
            width: 40px;
            height: 40px;
            border-radius: 12px;
            box-shadow: var(--shadow-md);
        }

        .nav-menu {
            display: flex;
            align-items: center;
            gap: 2rem;
            list-style: none;
        }

        .nav-link {
            text-decoration: none;
            color: var(--text);
            font-weight: 500;
            transition: all 0.3s ease;
            position: relative;
            padding: 0.5rem 1rem;
            border-radius: 8px;
        }

        .nav-link:hover {
            color: var(--accent);
            background: rgba(99, 102, 241, 0.1);
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 50%;
            width: 0;
            height: 2px;
            background: var(--gradient-primary);
            transition: all 0.3s ease;
            transform: translateX(-50%);
        }

        .nav-link:hover::after {
            width: 80%;
        }

        .cta-button {
            background: var(--gradient-primary);
            color: var(--text-white);
            padding: 0.75rem 1.5rem;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: var(--shadow-md);
            position: relative;
            overflow: hidden;
        }

        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-glow);
        }

        .cta-button:hover::before {
            left: 100%;
        }

        /* Contenido principal */
        .main-content {
            margin-top: 80px;
            position: relative;
            z-index: 10;
        }

        /* Keyframes para la animación de pulso del botón */
        @keyframes pulse {
            0%, 100% {
                box-shadow: 0 0 0 0 rgba(99, 102, 241, 0.7);
            }
            70% {
                box-shadow: 0 0 0 15px rgba(99, 102, 241, 0);
            }
        }

        /* Clase para aplicar la animación de pulso */
        .cta-pulse {
            animation: pulse 2s infinite;
        }

        /* Estilo del botón principal */
        .main-cta-button {
            background: var(--gradient-primary);
            color: var(--text-white);
            font-weight: 600;
            font-size: 1.1rem;
            padding: 1rem 2rem;
            border-radius: 12px;
            border: none;
            cursor: pointer;
            transition: all 0.3s ease;
            letter-spacing: 0.5px;
            box-shadow: var(--shadow-lg);
            position: relative;
            overflow: hidden;
        }

        .main-cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .main-cta-button:hover {
            transform: translateY(-3px);
            box-shadow: var(--shadow-glow);
        }

        .main-cta-button:hover::before {
            left: 100%;
        }

        /* Calendly responsive */
        .calendly-inline-widget {
            max-width: 100%;
            box-sizing: border-box;
        }

        #calendly-section {
            overflow-x: hidden;
        }

        /* Calendly horizontal en PC */
        @media (min-width: 769px) {
            .calendly-inline-widget {
                height: 630px !important;
                min-width: 100% !important;
            }
        }

        @media (max-width: 768px) {
            .calendly-inline-widget {
                height: 1000px !important;
                min-width: 100% !important;
            }
            
            /* Ajustar altura del video en móvil para formato vertical */
            #video-container .relative.w-full {
                height: 500px !important;
            }
            
            /* El video vertical debe centrarse y ocupar el espacio adecuadamente */
            #desktop-video {
                object-fit: contain !important;
            }
        }
        

        /* Hero Section */
        .hero {
            min-height: auto;
            padding: 1rem 0;
            background: var(--gradient-hero);
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grid" width="20" height="20" patternUnits="userSpaceOnUse"><path d="M 20 0 L 0 0 0 20" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="0.5"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
            opacity: 0.3;
        }

        .hero::after {
            content: '';
            position: absolute;
            top: 50%;
            right: -10%;
            width: 60%;
            height: 60%;
            background: radial-gradient(circle, rgba(99, 102, 241, 0.1) 0%, transparent 70%);
            border-radius: 50%;
            transform: translateY(-50%);
        }

        .hero-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: grid;
            gap: 4rem;
            align-items: center;
            position: relative;
            z-index: 2;
        }

        .hero-content h1 {
            font-size: 3.5rem;
            font-weight: 900;
            color: var(--text-white);
            margin-bottom: 1.5rem;
            line-height: 1.1;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        }

        .hero-content h1 span {
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            position: relative;
        }

        .hero-content h1 span::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 100%;
            height: 3px;
            background: var(--gradient-primary);
            border-radius: 2px;
        }

        .hero-content p {
            font-size: 1.35rem;
            color: rgba(255, 255, 255, 0.95);
            margin-bottom: 0;
            margin-top: 1.5rem;
            line-height: 1.6;
            font-weight: 500;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        }

        /* Section Styles */
        .section {
            padding: 6rem 0;
            position: relative;
        }

        .section-header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .section-title {
            font-size: 3rem;
            font-weight: 800;
            color: var(--text);
            margin-bottom: 1rem;
            background: var(--gradient-secondary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .section-subtitle {
            font-size: 1.25rem;
            color: var(--text-light);
            max-width: 600px;
            margin: 0 auto;
        }

        /* Card Styles */
        .card {
            background: var(--bg);
            padding: 2.5rem;
            border-radius: 24px;
            border: 1px solid var(--border-light);
            transition: all 0.3s ease;
            box-shadow: var(--shadow-md);
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--gradient-primary);
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-12px);
            box-shadow: var(--shadow-xl);
        }

        .card:hover::before {
            transform: scaleX(1);
        }

        /* Partners Section */
        .partners-section {
            padding: 6rem 0;
            background: var(--bg-secondary);
        }

        .partners-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .partners-card {
            background: var(--bg);
            padding: 3rem;
            border-radius: 24px;
            box-shadow: var(--shadow-md);
            border: 1px solid var(--border-light);
        }

        /* About Section */
        .about-section {
            padding: 6rem 0;
            background: var(--bg);
        }

        .about-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        /* Pillars Section */
        .pillars-section {
            padding: 6rem 0;
            background: var(--bg-secondary);
        }

        .pillars-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .pillar-card {
            background: var(--bg);
            padding: 3rem;
            border-radius: 24px;
            border: 1px solid var(--border-light);
            transition: all 0.3s ease;
            box-shadow: var(--shadow-md);
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .pillar-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--gradient-primary);
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .pillar-card:hover {
            transform: translateY(-12px);
            box-shadow: var(--shadow-xl);
        }

        .pillar-card:hover::before {
            transform: scaleX(1);
        }

        .pillar-icon {
            width: 80px;
            height: 80px;
            background: var(--gradient-primary);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
            box-shadow: var(--shadow-lg);
            transition: transform 0.3s ease;
        }

        .pillar-card:hover .pillar-icon {
            transform: scale(1.1) rotate(5deg);
        }

        .pillar-icon svg {
            width: 40px;
            height: 40px;
            color: var(--text-white);
        }

        /* CTA Section */
        .cta-section {
            padding: 6rem 0;
            background: var(--gradient-secondary);
            color: var(--text-white);
            position: relative;
            overflow: hidden;
        }

        .cta-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="dots" width="20" height="20" patternUnits="userSpaceOnUse"><circle cx="10" cy="10" r="1" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23dots)"/></svg>');
        }

        .cta-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 0 2rem;
            text-align: center;
            position: relative;
            z-index: 2;
        }

        .cta-title {
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 1.5rem;
        }

        .cta-description {
            font-size: 1.25rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }

        /* Footer */
        .footer {
            background: var(--bg-dark);
            color: var(--text-white);
            padding: 4rem 0 2rem;
        }

        .footer-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            text-align: center;
        }

        .footer-section h3 {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: var(--accent);
        }

        .footer-section p,
        .footer-section a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            line-height: 1.6;
            transition: color 0.3s ease;
        }

        .footer-section a:hover {
            color: var(--accent);
        }

        .footer-bottom {
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            margin-top: 3rem;
            padding-top: 2rem;
            text-align: center;
            color: rgba(255, 255, 255, 0.6);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .nav-menu {
                display: none;
            }

            .hero-container {
                grid-template-columns: 1fr;
                text-align: center;
                gap: 2rem;
            }

            .hero-content h1 {
                font-size: 1.75rem !important;
            }

            .hero-content p {
                font-size: 1.15rem !important;
            }

            .about-container {
                grid-template-columns: 1fr;
                gap: 2rem;
            }

            .section-title {
                font-size: 2.5rem;
            }
        }

        @media (max-width: 480px) {
            .hero-content h1 {
                font-size: 1.5rem !important;
            }

            .hero-content p {
                font-size: 1rem !important;
            }
        }

    </style>
</head>
<body class="text-white antialiased">
    <!-- Video Overlay para Móviles -->
    <div id="mobile-video-overlay" class="mobile-video-overlay">
        <div class="mobile-video-container">
            <button id="close-video" class="close-video-button" aria-label="Cerrar video">✕</button>
            <button id="toggle-audio" class="toggle-audio-button" aria-label="Silenciar sonido">
                🔊
            </button>
            <video 
                id="mobile-video" 
                class="mobile-video-player" 
                autoplay
                loop
                playsinline
                style="object-fit: cover;">
            </video>
        </div>
    </div>

    <!-- Header -->
    <header class="header" id="header">
        <nav class="nav-container">
            <a href="/" class="logo">
                <img src="/logo.png" alt="Scaleally Logo" />
                Scaleally
            </a>
            <ul class="nav-menu" style="display: none;">
            </ul>
            <a href="#calendly-section" class="cta-button">Agendar Consulta</a>
        </nav>
    </header>

    <!-- Contenido Principal -->
    <div class="main-content">
        <!-- VSL Hero Section -->
        <section class="hero" style="padding-top: 80px; padding-bottom: 1rem;">
            <div class="hero-container" style="grid-template-columns: 1fr;">
                <!-- Text Content Container -->
                <div class="hero-content" style="text-align: center; max-width: 900px; margin: 0 auto;">
            <!-- Main Headline -->
                    <h1 style="font-size: 2.5rem; font-weight: 900; color: var(--text-white); margin-bottom: 0; line-height: 1.2; text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);">
                        <span style="background: var(--gradient-primary); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
                            Método probado para escalar marcas de salud y bienestar en este 2025-2026
                        </span>
            </h1>
                    <p style="font-size: 1.35rem; color: rgba(255, 255, 255, 0.95); margin-top: 1.5rem; margin-bottom: 0; line-height: 1.6; font-weight: 500; text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);">
                        Descubre la estrategia que ha transformado cientos de empresas y multiplicado sus resultados exponencialmente
                    </p>
        </div>
            </div>
        </section>
        
        <section style="background: var(--bg); padding: 4rem 0;">
            <div style="max-width: 1200px; margin: 0 auto; padding: 0 2rem;">
                <!-- Video Player Container -->
                <div id="video-container" style="max-width: 900px; margin: 0 auto 3rem;">
                    <div class="card" style="padding: 1.5rem;">
                        <div style="position: relative;">
                            <div style="position: relative; width: 100%; border-radius: 12px; overflow: hidden; background: #000; height: 600px;">
                        <video 
                            id="desktop-video" 
                            controls
                            loop
                            playsinline
                            style="position: absolute; top: 0; left: 0; height: 100%; width: 100%; object-fit: contain;">
                        </video>
                    </div>
                </div>
            </div>
        </div>
            
            <!-- Primary CTA Button -->
                <div style="text-align: center; margin-bottom: 4rem;">
                <button id="primary-cta" class="main-cta-button cta-pulse">
                        Agenda tu Llamada
                </button>
            </div>
        </div>
    </section>

    <!-- Partners Section -->
    <section class="partners-section">
        <div class="partners-container">
            <div class="section-header">
                <h2 class="section-title">Partners</h2>
            </div>
            
            <div class="partners-card">
                <div style="display: flex; flex-wrap: wrap; justify-content: center; align-items: center; gap: 3rem; padding: 1rem;">
                    <div style="opacity: 0.7; transition: opacity 0.3s;">
                        <img src="/logoklaviyo.png" alt="Klaviyo" style="height: 60px; object-fit: contain;">
                </div>
                    <div style="opacity: 0.7; transition: opacity 0.3s;">
                        <img src="/logoslack.png" alt="Slack" style="height: 60px; object-fit: contain;">
                </div>
                    <div style="opacity: 0.7; transition: opacity 0.3s;">
                        <img src="/logon8n.png" alt="N8N" style="height: 60px; object-fit: contain;">
                </div>
                    <div style="opacity: 0.7; transition: opacity 0.3s;">
                        <img src="/logometa.png" alt="Meta" style="height: 60px; object-fit: contain;">
                </div>
                    <div style="opacity: 0.7; transition: opacity 0.3s;">
                        <img src="/logogoogle.png" alt="Google Ads" style="height: 60px; object-fit: contain;">
                </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Sobre Mí Section -->
    <section class="about-section" id="sobre-mi">
        <div class="about-container">
            <!-- Left: Logo Scaleally -->
            <div style="display: flex; justify-content: center;">
                <div class="card" style="width: 320px; height: 320px; display: flex; align-items: center; justify-content: center; padding: 2rem;">
                    <img src="/logo2.png" alt="Scaleally Logo" style="width: 100%; height: auto; max-width: 256px; object-fit: contain;">
                    </div>
                </div>
                
                <!-- Right: Text Content -->
            <div>
                <h2 class="section-title" style="text-align: left; margin-bottom: 1.5rem;">Sobre Mí</h2>
                <p style="color: var(--text-light); margin-bottom: 1rem; line-height: 1.8;">
                        Hola!, mi nombre es Miguel Rodriguez, Profesional en marketing y como tu growth partner quiero decirte que En un mercado lleno de ruido, las marcas que perduran son las que crean un impacto real, las que conectan y las que crecen.
                    </p>
                <p style="color: var(--text-light); margin-bottom: 1rem; line-height: 1.8;">
                        A través de una visión estratégica y creativa, elevamos tu proyecto para que resuene con tu audiencia a un nivel más profundo. Te ayudamos a pasar de ser una opción más a convertirte en una referencia en tu nicho, inspirando a una comunidad que cree en tu propósito.
                    </p>
                <p style="color: var(--text-light); margin-bottom: 2rem; line-height: 1.8;">
                        Hagamos que tu negocio crezca de forma sostenible y deje una huella de la que te sientas orgulloso.
                    </p>
                        <button id="sobre-mi-cta" class="main-cta-button">
                            Agenda tu Demo
                        </button>
            </div>
        </div>
    </section>

    <!-- Los 3 Pilares Section -->
    <section class="pillars-section" id="pilares">
        <div class="pillars-container">
            <!-- Header Section -->
            <div class="section-header">
                <h2 class="section-title">¿Cómo lo Hacemos?</h2>
                <p class="section-subtitle">Nuestro método probado para escalar tu negocio</p>
            </div>
            
            <!-- 3 Tarjetas de Pilares -->
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem;">
                <!-- Tarjeta 1: Atención Orgánica -->
                <div class="pillar-card">
                    <div class="pillar-icon">
                        <svg fill="currentColor" viewBox="0 0 20 20">
                            <path d="M10 2a6 6 0 00-6 6v3.586l-.707.707A1 1 0 004 14h12a1 1 0 00.707-1.707L16 11.586V8a6 6 0 00-6-6zM10 18a3 3 0 01-3-3h6a3 3 0 01-3 3z"/>
                        </svg>
                    </div>
                    <h3 style="font-size: 1.5rem; font-weight: 700; color: var(--text); margin-bottom: 1rem;">1. Atención Orgánica</h3>
                    <p style="color: var(--text-light); line-height: 1.8;">
                        Captar atención orgánica o con tráfico pagado, y mandar toda esta audiencia a un embudo de ventas apoyado en una oferta de contenido de alto valor.
                    </p>
                </div>
                
                <!-- Tarjeta 2: Oferta Ganadora -->
                <div class="pillar-card">
                    <div class="pillar-icon">
                        <svg fill="currentColor" viewBox="0 0 20 20">
                            <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                    </div>
                    <h3 style="font-size: 1.5rem; font-weight: 700; color: var(--text); margin-bottom: 1rem;">2. Oferta Ganadora</h3>
                    <p style="color: var(--text-light); line-height: 1.8;">
                        Liquidar esa atención y hacer que se convierta en venta apoyándonos en una oferta de adquisición ganadora.
                    </p>
                </div>
                
                <!-- Tarjeta 3: Proceso Post-Venta -->
                <div class="pillar-card">
                    <div class="pillar-icon">
                        <svg fill="currentColor" viewBox="0 0 20 20">
                            <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"/>
                            <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"/>
                        </svg>
                    </div>
                    <h3 style="font-size: 1.5rem; font-weight: 700; color: var(--text); margin-bottom: 1rem;">3. Proceso Post-Venta</h3>
                    <p style="color: var(--text-light); line-height: 1.8;">
                        Desarrollar un proceso post-venta para extender el valor de por vida de cada cliente utilizando marketing de email.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Calendly Section -->
    <section id="calendly-section" class="cta-section">
        <div class="cta-container">
            <h2 class="cta-title">📅 Agenda tu Consulta Gratuita de 15 Minutos</h2>
            <p class="cta-description">Descubre exactamente cómo aplicar nuestro método a tu empresa</p>
            
            <!-- Calendly Widget -->
            <div class="calendly-inline-widget" 
                 data-url="https://calendly.com/scaleally/30min"
                 style="min-width: 320px; width: 100%; border-radius: 12px; overflow: hidden;">
            </div>
        </div>
    </section>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-container">
            <div class="footer-section">
                <h3>Scaleally</h3>
                <p>Escalamos empresas de salud y bienestar.</p>
                <p style="margin-top: 1rem;"><a href="/contacto">Contacto</a></p>
                <p><a href="/politica-de-privacidad">Política de Privacidad</a></p>
            </div>
            <div class="footer-bottom">
                <p>&copy; {new Date().getFullYear()} Scaleally. Todos los derechos reservados.</p>
            </div>
        </div>
    </footer>

    <!-- HLS.js Library -->
    <script src="https://cdn.jsdelivr.net/npm/hls.js@latest"></script>
    
    <!-- Script para video en móviles -->
    <script>
        const mobileVideoUrl = 'https://vz-cddd003b-54d.b-cdn.net/d9b33ff9-2813-4bc1-8dd8-a9aeb3cd5e05/playlist.m3u8';
        const desktopVideoUrl = 'https://vz-cddd003b-54d.b-cdn.net/07cff805-1194-412c-87d0-dd024c07bd13/playlist.m3u8';

        // Detectar si es un dispositivo móvil
        function isMobileDevice() {
            return /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) 
                || (window.innerWidth <= 768);
        }

        // Inicializar video HLS
        function initHLS(videoElement: HTMLVideoElement | null, withSound = false, isMobile = false) {
            if (!videoElement) return;
            
            const videoUrl = isMobile ? mobileVideoUrl : desktopVideoUrl;

            // @ts-ignore - HLS.js is loaded from CDN
            if (typeof Hls !== 'undefined' && Hls.isSupported()) {
                // @ts-ignore
                const hls = new Hls({
                    enableWorker: true,
                    lowLatencyMode: true,
                    backBufferLength: 90
                });
                hls.loadSource(videoUrl);
                hls.attachMedia(videoElement);
                // @ts-ignore
                hls.on(Hls.Events.MANIFEST_PARSED, function() {
                    // Intentar reproducir con sonido si es móvil
                    if (withSound) {
                        videoElement.muted = false;
                        videoElement.play().catch((e: Error) => {
                            console.log('Autoplay con sonido bloqueado, intentando sin sonido');
                            // Si falla, intentar sin sonido
                            videoElement.muted = true;
                            videoElement.play().catch((err: Error) => console.log('Autoplay prevented:', err));
                            // Actualizar el botón
                            const toggleBtn = document.getElementById('toggle-audio');
                            if (toggleBtn) toggleBtn.textContent = '🔇';
                        });
                    } else {
                        videoElement.play().catch((e: Error) => console.log('Autoplay prevented:', e));
                    }
                });
            } else if (videoElement.canPlayType('application/vnd.apple.mpegurl')) {
                // Para Safari en iOS - usar la URL correcta
                const videoUrl = isMobile ? mobileVideoUrl : desktopVideoUrl;
                videoElement.src = videoUrl;
                videoElement.addEventListener('loadedmetadata', function() {
                    if (withSound) {
                        videoElement.muted = false;
                        videoElement.play().catch((e: Error) => {
                            console.log('Autoplay con sonido bloqueado, intentando sin sonido');
                            videoElement.muted = true;
                            videoElement.play().catch((err: Error) => console.log('Autoplay prevented:', err));
                            const toggleBtn = document.getElementById('toggle-audio');
                            if (toggleBtn) toggleBtn.textContent = '🔇';
                        });
                    } else {
                        videoElement.play().catch((e: Error) => console.log('Autoplay prevented:', e));
                    }
                });
            }
        }

        // Función para mostrar el video en pantalla completa
        function showMobileVideo() {
            const overlay = document.getElementById('mobile-video-overlay');
            const mobileVideo = document.getElementById('mobile-video') as HTMLVideoElement | null;
            
            if (overlay) {
                overlay.classList.add('active');
                document.body.classList.add('video-playing');
                
                // Inicializar HLS para el video móvil CON SONIDO
                if (mobileVideo && !mobileVideo.src) {
                    initHLS(mobileVideo, true, true); // true para sonido, true para móvil
                }
            }
        }

        // Función para cerrar el video
        function closeMobileVideo() {
            const overlay = document.getElementById('mobile-video-overlay');
            const mobileVideo = document.getElementById('mobile-video') as HTMLVideoElement | null;
            
            if (overlay) {
                overlay.classList.remove('active');
                document.body.classList.remove('video-playing');
                
                // Pausar el video móvil
                if (mobileVideo) {
                    mobileVideo.pause();
                }
                
                // Inicializar el video de la página principal si no está inicializado
                const desktopVideo = document.getElementById('desktop-video') as HTMLVideoElement | null;
                if (desktopVideo && !desktopVideo.src && !desktopVideo.dataset.initialized) {
                    desktopVideo.dataset.initialized = 'true';
                    // Usar el video móvil vertical para mantener consistencia
                    initHLS(desktopVideo, false, true);
                }
            }
        }

        // Ejecutar cuando el DOM esté listo
        document.addEventListener('DOMContentLoaded', function() {
            // Inicializar el video de escritorio automáticamente
            const desktopVideo = document.getElementById('desktop-video') as HTMLVideoElement | null;
            if (desktopVideo && !isMobileDevice()) {
                initHLS(desktopVideo, false, false); // sin sonido automático, desktop
            }
            
            // Agregar evento al botón de cerrar
            const closeButton = document.getElementById('close-video');
            if (closeButton) {
                closeButton.addEventListener('click', closeMobileVideo);
            }

            // Agregar evento al botón de audio
            const toggleAudioButton = document.getElementById('toggle-audio');
            const mobileVideo = document.getElementById('mobile-video') as HTMLVideoElement | null;
            
            if (toggleAudioButton && mobileVideo) {
                toggleAudioButton.addEventListener('click', function(e: MouseEvent) {
                    e.stopPropagation(); // Evitar que se cierre el overlay
                    if (mobileVideo.muted) {
                        mobileVideo.muted = false;
                        toggleAudioButton.textContent = '🔊';
                    } else {
                        mobileVideo.muted = true;
                        toggleAudioButton.textContent = '🔇';
                    }
                });

                // Permitir activar sonido tocando el video (si está silenciado)
                mobileVideo.addEventListener('click', function() {
                    if (mobileVideo.muted) {
                        mobileVideo.muted = false;
                        if (toggleAudioButton) toggleAudioButton.textContent = '🔊';
                    }
                });
            }

            // También permitir cerrar tocando fuera del video (opcional)
            const overlay = document.getElementById('mobile-video-overlay');
            if (overlay) {
                overlay.addEventListener('click', function(e: MouseEvent) {
                    if (e.target === overlay) {
                        closeMobileVideo();
                    }
                });
            }

            // Header scroll effect
            window.addEventListener('scroll', () => {
                const header = document.getElementById('header');
                if (header && window.scrollY > 100) {
                    header.classList.add('scrolled');
                } else if (header) {
                    header.classList.remove('scrolled');
                }
            });

            // Smooth scrolling for anchor links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', (e: Event) => {
                    e.preventDefault();
                    const href = anchor.getAttribute('href');
                    if (href) {
                        const target = document.querySelector(href);
                        if (target) {
                            target.scrollIntoView({
                                behavior: 'smooth',
                                block: 'start'
                            });
                        }
                    }
                });
            });

            // CTA buttons scroll to calendly
            const primaryCta = document.getElementById('primary-cta');
            const sobreMiCta = document.getElementById('sobre-mi-cta');
            const calendlySection = document.getElementById('calendly-section');

            if (primaryCta && calendlySection) {
                primaryCta.addEventListener('click', () => {
                    calendlySection.scrollIntoView({ behavior: 'smooth', block: 'start' });
                });
            }

            if (sobreMiCta && calendlySection) {
                sobreMiCta.addEventListener('click', () => {
                    calendlySection.scrollIntoView({ behavior: 'smooth', block: 'start' });
                });
            }
        });
    </script>
    
</body>
</html>
