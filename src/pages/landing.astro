---
// VSL Landing Page - Scaleally (Pure Video Sales Letter)
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zero to Multimillonaire - Método Provado para Escalar Marcas 2025-2026</title>
    <meta name="description" content="Descubre el método provado para escalar tu marca de zero a multimillonaire en 2025-2026. Sistema probado de crecimiento empresarial.">
    
    <!-- Calendly -->
    <script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
    
    <!-- Google Fonts - Premium Typography -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Oswald:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Import global styles -->
    <link rel="stylesheet" href="../styles/global.css">
    
    <!-- Import JavaScript -->
    <script src="../scripts/landing.js"></script>
    
    <style>
        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #ff6b35 0%, #f7931e 50%, #ff6b35 100%);
            background-attachment: fixed;
            position: relative;
            min-height: 100vh;
        }

        /* Video en pantalla completa para móviles */
        .mobile-video-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background-color: #000;
            z-index: 9999;
            overflow: hidden;
        }

        .mobile-video-overlay.active {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .mobile-video-container {
            width: 100%;
            height: 100%;
            position: relative;
        }

        .mobile-video-player {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .close-video-button {
            position: absolute;
            top: 20px;
            right: 20px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            border: 2px solid white;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            font-size: 24px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 10000;
            backdrop-filter: blur(5px);
            transition: all 0.3s ease;
        }

        .close-video-button:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: scale(1.1);
        }


        /* Ocultar el contenido principal cuando el video está activo en móviles */
        body.video-playing {
            overflow: hidden;
        }

        /* Fondo de montañas */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background: 
                linear-gradient(135deg, rgba(255, 107, 53, 0.8) 0%, rgba(247, 147, 30, 0.6) 50%, rgba(255, 107, 53, 0.8) 100%),
                url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 800"><defs><linearGradient id="sky" x1="0%" y1="0%" x2="100%" y2="100%"><stop offset="0%" style="stop-color:%23ff6b35;stop-opacity:1" /><stop offset="50%" style="stop-color:%23f7931e;stop-opacity:1" /><stop offset="100%" style="stop-color:%23ff6b35;stop-opacity:1" /></linearGradient></defs><rect width="1200" height="800" fill="url(%23sky)"/><path d="M0,600 Q200,400 400,500 T800,450 T1200,500 L1200,800 L0,800 Z" fill="%23333" opacity="0.8"/><path d="M0,650 Q300,450 600,550 T1200,500 L1200,800 L0,800 Z" fill="%23444" opacity="0.6"/></svg>');
            background-size: cover;
            background-position: center;
            z-index: -2;
        }

        /* Tira superior */
        .top-strip {
            background-color: rgba(26, 26, 26, 0.8);
            height: 60px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 2rem;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            border-bottom: 2px solid #ffd700;
            backdrop-filter: blur(10px);
        }

        .logo-text {
            color: #ffd700;
            font-family: 'Great Vibes', cursive;
            font-size: 2.5rem;
            font-weight: 400;
            letter-spacing: 0px;
            text-shadow: 0 3px 6px rgba(0, 0, 0, 0.4), 0 0 20px rgba(255, 215, 0, 0.3);
            transform: rotate(-2deg);
            display: inline-block;
            white-space: nowrap;
        }

        @media (max-width: 768px) {
            .logo-text {
                font-size: 1.5rem;
            }
        }

        @media (max-width: 480px) {
            .logo-text {
                font-size: 1.3rem;
            }
        }

        .strip-text {
            color: #ffd700;
            font-size: 0.9rem;
            font-weight: bold;
            letter-spacing: 1px;
        }

        /* Contenido principal */
        .main-content {
            margin-top: 60px;
            position: relative;
            z-index: 10;
        }

        /* Keyframes para la animación de pulso del botón */
        @keyframes pulse {
            0%, 100% {
                box-shadow: 0 0 0 0 rgba(147, 51, 234, 0.7);
            }
            70% {
                box-shadow: 0 0 0 15px rgba(147, 51, 234, 0);
            }
        }

        /* Clase para aplicar la animación de pulso */
        .cta-pulse {
            animation: pulse 2s infinite;
        }

        /* Estilo del botón principal */
        .main-cta-button {
            background: linear-gradient(135deg, #9333ea 0%, #7c3aed 100%);
            color: white;
            font-weight: bold;
            font-size: 1.1rem;
            padding: 1rem 2rem;
            border-radius: 50px;
            border: none;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            box-shadow: 0 4px 15px rgba(147, 51, 234, 0.3);
        }

        .main-cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(147, 51, 234, 0.4);
        }

        /* Calendly responsive */
        .calendly-inline-widget {
            max-width: 100%;
            box-sizing: border-box;
        }

        #calendly-section {
            overflow-x: hidden;
        }

        @media (max-width: 768px) {
            .calendly-inline-widget {
                height: 600px !important;
                min-width: 100% !important;
            }
        }
    </style>
</head>
<body class="text-white antialiased">
    <!-- Video Overlay para Móviles -->
    <div id="mobile-video-overlay" class="mobile-video-overlay">
        <div class="mobile-video-container">
            <button id="close-video" class="close-video-button" aria-label="Cerrar video">✕</button>
            <iframe 
                id="mobile-video" 
                class="mobile-video-player" 
                src="https://iframe.mediadelivery.net/play/458976/55b607f1-f156-4fbd-8c7d-21d0ee24aa67?autoplay=true&muted=true"
                loading="lazy"
                style="border: none; position: absolute; top: 0; left: 0; height: 100%; width: 100%;"
                allow="accelerometer; gyroscope; autoplay; encrypted-media; picture-in-picture;"
                allowfullscreen="true">
            </iframe>
        </div>
    </div>

    <!-- Tira Superior -->
    <div class="top-strip">
        <div class="logo-text">Miguel Rodriguez</div>
        <div class="strip-text">GROWTH PARTNER</div>
    </div>

    <!-- Contenido Principal -->
    <div class="main-content">
        <!-- VSL Hero Section -->
        <section class="min-h-screen flex flex-col items-center justify-center px-4 py-20 relative z-10">
        <!-- Text Content Container (Wider) -->
        <div class="max-w-6xl mx-auto text-center mb-12">
            
            <!-- Main Headline -->
            <h1 class="text-4xl md:text-5xl lg:text-6xl font-black leading-tight mb-8 text-white" style="font-family: 'Oswald', sans-serif; letter-spacing: -1px;">
                ZERO TO MULTIMILLONAIRE<br>
                <span class="text-3xl md:text-4xl lg:text-5xl">por método provado para escalar marcas en este 2025-2026</span>
            </h1>
        </div>
        
        <!-- Video Player Container (Compact Size) -->
        <div class="w-full max-w-4xl mx-auto mb-12">
            <div class="bg-white rounded-xl p-4 shadow-2xl">
                <div class="relative">
                    <!-- Video de Bunny.net -->
                    <div class="relative w-full h-[400px] md:h-[450px] rounded-lg overflow-hidden bg-black">
                        <iframe 
                            id="desktop-video" 
                            src="https://iframe.mediadelivery.net/play/458976/55b607f1-f156-4fbd-8c7d-21d0ee24aa67"
                            loading="lazy"
                            style="border: none; position: absolute; top: 0; left: 0; height: 100%; width: 100%;"
                            allow="accelerometer; gyroscope; autoplay; encrypted-media; picture-in-picture;"
                            allowfullscreen="true">
                        </iframe>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Content Container (Wider) -->
        <div class="max-w-6xl mx-auto text-center">
            
            <!-- Primary CTA Button -->
            <div class="mb-8">
                <button id="primary-cta" class="main-cta-button cta-pulse">
                    AGENDA TU LLAMADA
                </button>
            </div>
            
        </div>
    </section>

    <!-- Partners Section -->
    <section class="w-full py-16 bg-black bg-opacity-30">
        <div class="max-w-6xl mx-auto px-4">
            <!-- Header -->
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold text-white uppercase tracking-wide">Partners</h2>
            </div>
            
            <!-- Partners Logos -->
            <div class="flex flex-wrap justify-center items-center gap-8 md:gap-12 lg:gap-16">
                <!-- Klaviyo -->
                <div class="opacity-70 hover:opacity-100 transition-opacity duration-300">
                    <img src="/logoklaviyo.png" alt="Klaviyo" class="h-12 md:h-16 object-contain">
                </div>
                
                <!-- Slack -->
                <div class="opacity-70 hover:opacity-100 transition-opacity duration-300">
                    <img src="/logoslack.png" alt="Slack" class="h-12 md:h-16 object-contain">
                </div>
                
                <!-- N8N -->
                <div class="opacity-70 hover:opacity-100 transition-opacity duration-300">
                    <img src="/logon8n.png" alt="N8N" class="h-12 md:h-16 object-contain">
                </div>
                
                <!-- Meta -->
                <div class="opacity-70 hover:opacity-100 transition-opacity duration-300">
                    <img src="/logometa.png" alt="Meta" class="h-12 md:h-16 object-contain">
                </div>
                
                <!-- Google Ads -->
                <div class="opacity-70 hover:opacity-100 transition-opacity duration-300">
                    <img src="/logogoogle.png" alt="Google Ads" class="h-12 md:h-16 object-contain">
                </div>
            </div>
        </div>
    </section>

    <!-- Sobre Mí Section -->
    <section class="w-full py-20 bg-black bg-opacity-30">
        <div class="max-w-7xl mx-auto px-4">
            <div class="grid md:grid-cols-2 gap-12 items-center">
                <!-- Left: Logo Scaleally -->
                <div class="flex justify-center">
                    <div class="w-80 h-80 flex items-center justify-center">
                        <img src="/logo2.png" alt="Scaleally Logo" class="w-full h-auto max-w-64 object-contain">
                    </div>
                </div>
                
                <!-- Right: Text Content -->
                <div class="text-left">
                    <h2 class="text-4xl font-bold text-white mb-6">SOBRE MÍ</h2>
                    <p class="text-base text-gray-300 mb-4 leading-relaxed">
                        Hola!, mi nombre es Miguel Rodriguez, Profesional en marketing y como tu growth partner quiero decirte que En un mercado lleno de ruido, las marcas que perduran son las que crean un impacto real. Más que marketing, nos dedicamos a construir legados.
                    </p>
                    <p class="text-base text-gray-300 mb-4 leading-relaxed">
                        A través de una visión estratégica y creativa, elevamos tu proyecto para que resuene con tu audiencia a un nivel más profundo. Te ayudamos a pasar de ser una opción más a convertirte en la referencia principal en tu nicho, inspirando a una comunidad que cree en tu propósito.
                    </p>
                    <p class="text-base text-gray-300 mb-6 leading-relaxed">
                        Hagamos que tu negocio crezca de forma sostenible y deje una huella de la que te sientas orgulloso.
                    </p>
                    <div class="mt-8">
                        <button id="sobre-mi-cta" class="main-cta-button">
                            Agenda tu Demo
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Los 3 Pilares Section -->
    <section class="w-full py-24 bg-black bg-opacity-40">
        <div class="max-w-7xl mx-auto px-4">
            <!-- Header Section -->
            <div class="text-center mb-20">
                <h2 class="text-5xl md:text-6xl font-black text-white mb-8 leading-tight">
                    ¿CÓMO LO HACEMOS?
                </h2>
            </div>
            
            <!-- 3 Tarjetas de Pilares -->
            <div class="grid md:grid-cols-3 gap-8 lg:gap-12">
                <!-- Tarjeta 1: Atención Orgánica -->
                <div class="bg-black bg-opacity-50 rounded-xl shadow-lg p-8 text-center border border-gray-600">
                    <!-- Icono -->
                    <div class="w-16 h-16 bg-purple-500 rounded-lg flex items-center justify-center mx-auto mb-6">
                        <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M10 2a6 6 0 00-6 6v3.586l-.707.707A1 1 0 004 14h12a1 1 0 00.707-1.707L16 11.586V8a6 6 0 00-6-6zM10 18a3 3 0 01-3-3h6a3 3 0 01-3 3z"/>
                        </svg>
                    </div>
                    
                    <!-- Título -->
                    <h3 class="text-xl font-bold text-white mb-4">1. Atención orgánica</h3>
                    
                    <!-- Descripción -->
                    <p class="text-gray-300 leading-relaxed">
                        Captar atención orgánica o con tráfico pagado, y mandar toda esta audiencia a un embudo de ventas apoyado en una oferta de contenido de alto valor.
                    </p>
                </div>
                
                <!-- Tarjeta 2: Oferta Ganadora -->
                <div class="bg-black bg-opacity-50 rounded-xl shadow-lg p-8 text-center border border-gray-600">
                    <!-- Icono -->
                    <div class="w-16 h-16 bg-purple-500 rounded-lg flex items-center justify-center mx-auto mb-6">
                        <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                    </div>
                    
                    <!-- Título -->
                    <h3 class="text-xl font-bold text-white mb-4">2. Oferta Ganadora</h3>
                    
                    <!-- Descripción -->
                    <p class="text-gray-300 leading-relaxed">
                        Liquidar esa atención y hacer que se convierta en venta apoyándonos en una oferta de adquisición ganadora.
                    </p>
                </div>
                
                <!-- Tarjeta 3: Proceso Post-Venta -->
                <div class="bg-black bg-opacity-50 rounded-xl shadow-lg p-8 text-center border border-gray-600">
                    <!-- Icono -->
                    <div class="w-16 h-16 bg-purple-500 rounded-lg flex items-center justify-center mx-auto mb-6">
                        <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"/>
                            <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"/>
                        </svg>
                    </div>
                    
                    <!-- Título -->
                    <h3 class="text-xl font-bold text-white mb-4">3. Proceso Post-Venta</h3>
                    
                    <!-- Descripción -->
                    <p class="text-gray-300 leading-relaxed">
                        Desarrollar un proceso post-venta para extender el valor de por vida de cada cliente utilizando marketing de email.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Calendly Section -->
    <section id="calendly-section" class="w-full py-20 bg-black bg-opacity-50">
        <div class="max-w-6xl mx-auto px-4">
            <h3 class="text-3xl font-bold mb-6 text-center text-white">📅 Agenda tu Consulta Gratuita de 15 Minutos</h3>
            <p class="text-gray-300 mb-8 text-center text-lg">Descubre exactamente cómo aplicar nuestro método a tu empresa</p>
            
            <!-- Calendly Widget -->
            <div class="calendly-inline-widget rounded-2xl overflow-hidden" 
                 data-url="https://calendly.com/scaleally/30min"
                 style="min-width: 320px; width: 100%; height: 700px;">
            </div>
        </div>
    </section>
    </div>

    <!-- Footer -->
    <footer class="bg-black text-gray-400 py-12 text-center">
        <div class="max-w-2xl mx-auto">
            <div class="text-3xl font-black text-cyan-400 mb-4">Scaleally</div>
            <p class="text-gray-500">
                Escalamos empresas de salud y bienestar.
            </p>
        </div>
    </footer>

    <!-- Script para video en móviles -->
    <script>
        // Detectar si es un dispositivo móvil
        function isMobileDevice() {
            return /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) 
                || (window.innerWidth <= 768);
        }

        // Función para mostrar el video en pantalla completa
        function showMobileVideo() {
            const overlay = document.getElementById('mobile-video-overlay');
            
            if (overlay) {
                overlay.classList.add('active');
                document.body.classList.add('video-playing');
            }
        }

        // Función para cerrar el video
        function closeMobileVideo() {
            const overlay = document.getElementById('mobile-video-overlay');
            
            if (overlay) {
                overlay.classList.remove('active');
                document.body.classList.remove('video-playing');
                
                // Recargar el iframe para detener el video
                const iframe = document.getElementById('mobile-video') as HTMLIFrameElement;
                if (iframe) {
                    const src = iframe.src;
                    iframe.src = '';
                    setTimeout(() => {
                        iframe.src = src;
                    }, 100);
                }
            }
        }

        // Ejecutar cuando el DOM esté listo
        document.addEventListener('DOMContentLoaded', function() {
            // Si es móvil, mostrar el video automáticamente
            if (isMobileDevice()) {
                // Esperar un momento para asegurar que todo esté cargado
                setTimeout(showMobileVideo, 500);
            }

            // Agregar evento al botón de cerrar
            const closeButton = document.getElementById('close-video');
            if (closeButton) {
                closeButton.addEventListener('click', closeMobileVideo);
            }

            // También permitir cerrar tocando fuera del video (opcional)
            const overlay = document.getElementById('mobile-video-overlay');
            if (overlay) {
                overlay.addEventListener('click', function(e) {
                    if (e.target === overlay) {
                        closeMobileVideo();
                    }
                });
            }
        });
    </script>
    
</body>
</html>
