---
---
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <title>Solicitar Eliminación de Datos | Scaleally</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <style>
			:root {
				--primary: #1a1a2e;
				--secondary: #16213e;
				--accent: #6366f1;
				--accent-hover: #4f46e5;
				--accent-light: #818cf8;
				--success: #10b981;
				--warning: #f59e0b;
				--error: #ef4444;
				--bg: #ffffff;
				--bg-secondary: #f8fafc;
				--bg-dark: #0f0f23;
				--text: #1a1a2e;
				--text-light: #64748b;
				--text-white: #ffffff;
				--border: #e2e8f0;
				--border-light: #f1f5f9;
				--shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
				--shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
				--shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
				--shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
				--shadow-glow: 0 0 20px rgba(99, 102, 241, 0.3);
				--gradient-primary: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
				--gradient-secondary: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
				--gradient-accent: linear-gradient(135deg, #6366f1 0%, #818cf8 100%);
				--gradient-glow: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
			}
      body { 
        font-family: 'Inter', Arial, sans-serif; 
        background: #f8fafc; 
        color: #18181b; 
        margin: 0; 
        line-height: 1.6;
      }
      .main { 
        max-width: 600px; 
        margin: 0 auto; 
        padding: 3rem 1rem 2rem 1rem; 
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 2px 24px #7c3aed11;
        margin-top: 2rem;
        margin-bottom: 2rem;
      }
             .title { 
         color: #1e293b; 
         font-size: 2.2rem; 
         font-weight: 800; 
         margin-bottom: 1.5rem; 
         text-align: center;
       }
             .back { 
         display: inline-block; 
         margin-bottom: 2rem; 
         color: #f59e0b; 
         text-decoration: none; 
         font-weight: 600; 
       }
      .back:hover {
        text-decoration: underline;
      }
      .form-group {
        margin-bottom: 1.5rem;
      }
      .form-group label {
        display: block;
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: #374151;
      }
      .form-group input,
      .form-group textarea,
      .form-group select {
        width: 100%;
        padding: 0.75rem;
        border: 2px solid #e5e7eb;
        border-radius: 8px;
        font-size: 1rem;
        font-family: inherit;
        transition: border-color 0.2s;
        box-sizing: border-box;
      }
             .form-group input:focus,
       .form-group textarea:focus,
       .form-group select:focus {
         outline: none;
         border-color: #f59e0b;
         box-shadow: 0 0 0 3px rgba(245, 158, 11, 0.1);
       }
      .form-group textarea {
        resize: vertical;
        min-height: 100px;
      }
      .required {
        color: #dc2626;
      }
      .info-box {
        background: #fef3c7;
        color: #92400e;
        padding: 1rem;
        border-radius: 8px;
        margin: 1.5rem 0;
        border-left: 4px solid #f59e0b;
      }
             .submit-btn {
         background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
         color: #fff;
         font-weight: 600;
         font-size: 1.1rem;
         border: none;
         border-radius: 8px;
         padding: 1rem 2rem;
         cursor: pointer;
         width: 100%;
         transition: all 0.3s ease;
         margin-top: 1rem;
       }
             .submit-btn:hover {
         transform: translateY(-2px);
         box-shadow: 0 4px 20px rgba(245, 158, 11, 0.3);
       }
             .contact-info {
         background: #f8fafc;
         padding: 1.5rem;
         border-radius: 8px;
         margin-top: 2rem;
         border-left: 4px solid #f59e0b;
         text-align: center;
       }
             .contact-info strong {
         color: #f59e0b;
       }
             .contact-info a {
         color: #f59e0b;
         text-decoration: none;
       }
      .contact-info a:hover {
        text-decoration: underline;
      }
      @media (max-width: 700px) { 
        .main { 
          padding: 1.5rem 1rem; 
          margin: 1rem;
        } 
        .title {
          font-size: 1.8rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="main">
      <a href="/" class="back">← Volver al inicio</a>
      
      <div class="title">🗑️ Solicitar Eliminación de Datos</div>
      
      <div class="info-box">
        <strong>Información importante:</strong><br/>
        • Procesaremos tu solicitud en un máximo de 30 días<br/>
        • Necesitamos verificar tu identidad para proteger tus datos<br/>
        • Algunos datos pueden conservarse por obligaciones legales
      </div>
      
      <form id="delete-form">
        
        <div class="form-group">
          <label for="nombre">Nombre completo <span class="required">*</span></label>
          <input type="text" id="nombre" name="nombre" required placeholder="Tu nombre completo">
        </div>
        
        <div class="form-group">
          <label for="email">Correo electrónico <span class="required">*</span></label>
          <input type="email" id="email" name="email" required placeholder="tu@email.com">
        </div>
        
        <div class="form-group">
          <label for="telefono">Número de teléfono</label>
          <input type="tel" id="telefono" name="telefono" placeholder="+57 300 123 4567">
        </div>
        
        <div class="form-group">
          <label for="tipo-datos">Tipo de datos a eliminar <span class="required">*</span></label>
          <select id="tipo-datos" name="tipo-datos" required>
            <option value="">Selecciona una opción</option>
            <option value="todos">Todos mis datos personales</option>
            <option value="contacto">Solo información de contacto</option>
            <option value="mensajes">Solo mensajes y consultas</option>
            <option value="whatsapp">Solo datos de WhatsApp</option>
            <option value="consultoria">Solo datos de consultorías</option>
          </select>
        </div>
        
        <div class="form-group">
          <label for="razon">Motivo de la solicitud</label>
          <select id="razon" name="razon">
            <option value="">Selecciona una opción (opcional)</option>
            <option value="privacidad">Protección de privacidad</option>
            <option value="no-uso">Ya no uso los servicios</option>
            <option value="solicitud">Solicitud de usuario</option>
            <option value="otro">Otro motivo</option>
          </select>
        </div>
        
        <div class="form-group">
          <label for="comentarios">Comentarios adicionales</label>
          <textarea id="comentarios" name="comentarios" placeholder="Información adicional que nos ayude a procesar tu solicitud..."></textarea>
        </div>
        
        <div class="form-group">
          <label>
            <input type="checkbox" name="notificar-usuario" style="width: auto; margin-right: 0.5rem;">
            Quiero recibir una confirmación por email cuando se complete la eliminación
          </label>
        </div>
        
        <div class="form-group">
          <label>
            <input type="checkbox" name="confirmacion" required style="width: auto; margin-right: 0.5rem;">
            Confirmo que quiero eliminar mis datos personales y entiendo que este proceso no se puede deshacer <span class="required">*</span>
          </label>
        </div>
        
        <button type="submit" class="submit-btn">Enviar Solicitud de Eliminación</button>
      </form>
      
      <div class="contact-info">
        <strong>¿Necesitas ayuda?</strong><br/>
        Contáctanos antes de enviar tu solicitud:<br/><br/>
        <strong>Email:</strong> <a href="mailto:scaleally@gmail.com">scaleally@gmail.com</a><br/>
        <strong>WhatsApp:</strong> <a href="https://wa.me/573015699864" target="_blank" rel="noopener">+57 301-569-9864</a>
      </div>
      
      <div style="margin-top: 2rem; font-size: 0.9rem; color: #666; text-align: center;">
        <a href="/politica-de-privacidad" style="color: #7c3aed; text-decoration: none;">← Ver Política de Privacidad</a>
      </div>
    </div>

    <script>
      document.getElementById("delete-form").addEventListener("submit", async function (e) {
        e.preventDefault();

        const submitBtn = document.querySelector('.submit-btn');
        const originalText = submitBtn.textContent;
        submitBtn.textContent = 'Enviando...';
        submitBtn.disabled = true;

        try {
          const code = Math.random().toString(36).substring(2, 10).toUpperCase(); // código único
          const form = e.target;
          const data = new FormData(form);

          // agregamos el código al form
          data.append("confirmation_code", code);
          data.append("_subject", "🗑️ Solicitud de eliminación de datos - Scaleally");

          // envía los datos a Formspree
          const response = await fetch("https://formspree.io/f/xpwrvdqd", {
            method: "POST",
            body: data,
            headers: {
              Accept: "application/json",
            },
          });

          if (response.ok) {
            // redirige al usuario a tu página
            window.location.href = `/eliminar-datos?id=${code}`;
          } else {
            throw new Error('Error en el envío');
          }
        } catch (error) {
          console.error('Error:', error);
          alert('Error al enviar la solicitud. Por favor, intenta de nuevo.');
          submitBtn.textContent = originalText;
          submitBtn.disabled = false;
        }
      });
    </script>
  </body>
</html> 