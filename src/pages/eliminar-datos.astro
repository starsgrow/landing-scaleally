---
// El código de confirmación se mostrará con JS
const url = `https://scaleally.co/eliminar-datos`;
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Solicitud de eliminación de datos</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body {
        font-family: sans-serif;
        max-width: 600px;
        margin: 80px auto;
        padding: 0 20px;
        color: #333;
        text-align: center;
      }
      code {
        background: #f2f2f2;
        padding: 4px 8px;
        border-radius: 5px;
        display: inline-block;
      }
      .info-box {
        background: #f8fafc;
        border-left: 4px solid #f59e0b;
        padding: 1.2em 1em;
        margin: 2em 0 1.5em 0;
        border-radius: 10px;
        text-align: left;
        color: #222;
        font-size: 1.08em;
      }
			:root {
				--primary: #1a1a2e;
				--secondary: #16213e;
				--accent: #6366f1;
				--accent-hover: #4f46e5;
				--accent-light: #818cf8;
				--success: #10b981;
				--warning: #f59e0b;
				--error: #ef4444;
				--bg: #ffffff;
				--bg-secondary: #f8fafc;
				--bg-dark: #0f0f23;
				--text: #1a1a2e;
				--text-light: #64748b;
				--text-white: #ffffff;
				--border: #e2e8f0;
				--border-light: #f1f5f9;
				--shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
				--shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
				--shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
				--shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
				--shadow-glow: 0 0 20px rgba(99, 102, 241, 0.3);
				--gradient-primary: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
				--gradient-secondary: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
				--gradient-accent: linear-gradient(135deg, #6366f1 0%, #818cf8 100%);
				--gradient-glow: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
			}
    </style>
  </head>
  <body>
    <h1>🗑️ Solicitud de Eliminación Recibida</h1>
    <p>Tu solicitud fue registrada correctamente.</p>
    <p><strong>Código de confirmación:</strong></p>
    <p><code id="confirmation-code">cargando...</code></p>

    <p>✉️ Guarda este código de confirmación para futuras consultas.</p>

    <div class="info-box">
      🛠️ <b>Estamos trabajando en tu solicitud de eliminación de datos.</b><br>
      Te enviaremos un correo electrónico una vez hayamos finalizado con el proceso.<br><br>
      📨 <b>También puedes consultar el estado de tu solicitud escribiéndonos a:</b><br>
      <a href="mailto:contacto@scaleally.co">contacto@scaleally.co</a> o <a href="mailto:scaleally@gmail.com">scaleally@gmail.com</a><br><br>
      🔖 <b>En el asunto del correo, por favor indica:</b><br>
      <span style="color:#f59e0b;">Estado de solicitud de eliminación – [Tu código de confirmación]</span>
    </div>

    <p style="margin-top:2em; color:#f59e0b; font-weight:600;">🙏 Gracias por confiar en Scaleally. Estamos aquí para apoyarte siempre.</p>

    <script>
      const params = new URLSearchParams(window.location.search);
      const code = params.get('id') || 'desconocido';
      document.getElementById('confirmation-code').textContent = code;
    </script>
  </body>
</html> 